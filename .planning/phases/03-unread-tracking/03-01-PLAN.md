---
phase: 03-unread-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes.py
  - static/css/style.css
autonomous: true
requirements: [FILT-03]

must_haves:
  truths:
    - "API endpoint exists to mark conversation as read"
    - "Unread conversations have distinct visual styling (blue dot, border, bold name)"
    - "Screen reader users can identify unread conversations"
  artifacts:
    - path: "routes.py"
      provides: "PATCH /api/conversations/{id}/read endpoint"
      contains: "api_mark_conversation_read"
    - path: "static/css/style.css"
      provides: "Unread indicator styling"
      contains: ".conversation-card.unread"
  key_links:
    - from: "routes.py"
      to: "Conversation.is_read"
      via: "SQLAlchemy update"
      pattern: "is_read\\s*=\\s*True"
---

<objective>
Add mark-as-read API endpoint and CSS styling for unread conversation indicators.

Purpose: Enable backend support for marking conversations as read and provide visual styling for unread indicators that will be applied by JavaScript in Plan 02.
Output: Working API endpoint at PATCH /api/conversations/{id}/read, CSS classes for unread styling including accessibility support.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-unread-tracking/03-RESEARCH.md
@routes.py
@static/css/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mark-as-read API endpoint</name>
  <files>routes.py</files>
  <action>
Add a new PATCH endpoint to mark a conversation as read. Insert it in the API ROUTES section after the existing conversation endpoints (around line 98, after `api_get_messages`).

Endpoint: `PATCH /api/conversations/<int:conversation_id>/read`

Implementation:
1. Use `@chatbot_bp.route('/api/conversations/<int:conversation_id>/read', methods=['PATCH'])`
2. Name function `api_mark_conversation_read`
3. Get conversation via `Conversation.query.get_or_404(conversation_id)`
4. Set `conversation.is_read = True`
5. Commit the change
6. Return `jsonify({'success': True, 'is_read': True})`

No request body needed - this is a simple state change. Use 200 status (default for jsonify).
  </action>
  <verify>
Start the Flask server, then run:
```bash
curl -X PATCH http://localhost:5000/chatbot/api/conversations/1/read
```
Should return: `{"is_read": true, "success": true}`

Verify in database that conversation 1 has is_read=1.
  </verify>
  <done>PATCH /api/conversations/{id}/read endpoint returns success JSON and updates database</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for unread conversation indicators</name>
  <files>static/css/style.css</files>
  <action>
Add CSS styling for unread conversations at the end of the "Conversation List (Inbox)" section (after line 433, after `.status-badge.closed`).

Add these classes:

1. **Unread card styling** - `.conversation-card.unread`:
   - `position: relative;` (for positioning the dot)
   - `background: var(--bg-primary);` (subtle background difference)
   - `border-left: 3px solid var(--primary-color);` (left border indicator)

2. **Blue dot indicator** - `.conversation-card.unread::before`:
   - `content: '';`
   - `position: absolute;`
   - `left: 8px;` (position inside the card padding)
   - `top: 50%;`
   - `transform: translateY(-50%);`
   - `width: 8px;`
   - `height: 8px;`
   - `background: var(--primary-color);`
   - `border-radius: 50%;`

3. **Bold guest name for unread** - `.conversation-card.unread .guest-name`:
   - `font-weight: 700;` (bolder than default 600)

4. **Screen reader only class** - `.sr-only` (for accessibility):
   - `position: absolute;`
   - `width: 1px;`
   - `height: 1px;`
   - `padding: 0;`
   - `margin: -1px;`
   - `overflow: hidden;`
   - `clip: rect(0, 0, 0, 0);`
   - `white-space: nowrap;`
   - `border: 0;`

Note: The `.conversation-card` already has `padding: 16px` so the dot at `left: 8px` will be visible inside the padding area.
  </action>
  <verify>
Manually add `unread` class to a conversation card in inbox.html (temporarily), reload page, and verify:
1. Blue dot appears on left side of card
2. Left border is blue
3. Background is slightly different
4. Guest name is bolder

Remove the temporary class after verification.
  </verify>
  <done>CSS classes exist: .conversation-card.unread (with ::before dot), .conversation-card.unread .guest-name, .sr-only</done>
</task>

</tasks>

<verification>
1. API endpoint responds correctly:
   - `curl -X PATCH http://localhost:5000/chatbot/api/conversations/1/read` returns success
   - Non-existent conversation returns 404

2. CSS styling works:
   - Add `unread` class manually to a card in DevTools
   - Blue dot visible, left border blue, background different, name bolder
</verification>

<success_criteria>
1. PATCH /api/conversations/{id}/read endpoint exists and marks conversation as read
2. CSS classes for unread styling are defined and visually correct
3. Accessibility: .sr-only class exists for screen reader text
</success_criteria>

<output>
After completion, create `.planning/phases/03-unread-tracking/03-01-SUMMARY.md`
</output>
